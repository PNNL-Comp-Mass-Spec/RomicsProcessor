% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_Grouping-WE45756.R, R/07_Grouping.R
\name{romicsKmeansSamples}
\alias{romicsKmeansSamples}
\title{romicsKmeansSamples()}
\usage{
romicsKmeansSamples(
  romics_object,
  centers = 3,
  cluster_using = c("data", "pca", "umap", "tsne"),
  factor_name = "K_clust",
  iter.max = 100,
  algorithm = c("Lloyd", "Hartigan-Wong", "Forgy", "MacQueen"),
  lock.seed = TRUE,
  seed = 42,
  ...
)

romicsKmeansSamples(
  romics_object,
  centers = 3,
  cluster_using = c("data", "pca", "umap", "tsne"),
  factor_name = "K_clust",
  iter.max = 100,
  algorithm = c("Lloyd", "Hartigan-Wong", "Forgy", "MacQueen"),
  lock.seed = TRUE,
  seed = 42,
  ...
)
}
\arguments{

\item{romics_object}{A romics_object created using romicsCreateObject()}

\item{centers}{Either the number of clusters, or a set of initial (distinct) cluster centers.
If a number, a random set of (distinct) rows in the data is chosen as the initial centers.}

\item{cluster_using}{Character string specifying which data to use for clustering:
'data' (full data matrix), 'pca' (PCA embeddings), 'umap' (UMAP embeddings), or 'tsne' (t-SNE embeddings). Default: 'data'}

\item{factor_name}{Character string specifying the name for the new factor to be added to metadata.
Default: 'K_clust'}

\item{iter.max}{The maximum number of iterations allowed. Default: 100}

\item{algorithm}{Character string specifying the K-means algorithm to use:
'Hartigan-Wong', 'Lloyd', 'Forgy', or 'MacQueen'. Default: 'Lloyd'}

\item{lock.seed}{Logical. If TRUE, uses a fixed seed for reproducible clustering. Default: TRUE}

\item{seed}{Numeric value for the random seed when lock.seed=TRUE. Default: 42}

\item{...}{Additional parameters passed to the kmeans() function.}
}
\value{
Returns the romics_object with a new factor added to metadata containing the cluster assignments.
        Clusters are named as "{factor_name}_001", "{factor_name}_002", etc.
        The "colors_romics" factor is guaranteed to be the last row in metadata.

Returns the romics_object with a new factor added to metadata containing the cluster assignments.
        Clusters are named as "{factor_name}_001", "{factor_name}_002", etc.
        The "colors_romics" factor is guaranteed to be the last row in metadata.
}
\description{
Generate K-means clustering for the samples in a romics_object. The clustering can be based on
the full data matrix or dimensionally reduced data from PCA, UMAP, or t-SNE embeddings.

Generate K-means clustering for the samples in a romics_object. The clustering can be based on
the full data matrix or dimensionally reduced data from PCA, UMAP, or t-SNE embeddings.
}
\details{
This function performs K-means clustering on samples and adds the cluster assignments
         as a new factor in the romics_object's metadata. Using dimensionally-reduced data
         (PCA, UMAP, or t-SNE) can improve computation time but may reduce the accuracy of the clusters.
         The function ensures that "colors_romics" factor remains the last row in the metadata.
         Any NA values in cluster assignments will be replaced with "undefined cluster".

This function performs K-means clustering on samples and adds the cluster assignments
         as a new factor in the romics_object's metadata. Using dimensionally-reduced data
         (PCA, UMAP, or t-SNE) can improve computation time but may reduce the accuracy of the clusters.
         The function ensures that "colors_romics" factor remains the last row in the metadata.
         Any NA values in cluster assignments will be replaced with "undefined cluster".
}
\author{
Geremy Clair
}
