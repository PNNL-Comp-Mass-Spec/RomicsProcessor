% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/19_metadata_plots_and_operations.R
\name{romicsFactorProportionTestPairwise}
\alias{romicsFactorProportionTestPairwise}
\title{romicsFactorProportionTestPairwise}
\usage{
romicsFactorProportionTestPairwise(
  romics_object,
  test_factor,
  condition_factor = "condition",
  method = "chisq",
  simulate_p = TRUE,
  B = 10000,
  padj_method = "BH",
  residual_threshold = 2,
  min_effect_size = 5,
  min_fold_change = 1.5
)
}
\arguments{

\item{romics_object}{An romics_object created using romicsCreateObject().}

\item{test_factor}{A character string indicating the factor whose level proportions
will be tested (e.g., "Leiden_clust_names", "cell_type", "AGER_presence").}

\item{condition_factor}{A character string indicating the condition factor (e.g., "condition").}

\item{method}{A character string specifying the test method: "chisq" (chi-square test) or
"fisher" (Fisher's exact test via monte-carlo simulation for tables > 2x2). Default: "chisq".}

\item{simulate_p}{A logical indicating whether to simulate p-values (recommended when expected
cell frequencies are low, i.e. < 5). Default: TRUE.}

\item{B}{Integer. Number of replicates for p-value simulation. Default: 10000.}

\item{padj_method}{Correction method for multiple testing across pairwise comparisons. Default: "BH".}

\item{residual_threshold}{Minimum absolute standardized Pearson residual required to flag a
level as meaningfully over/under-represented. Conventionally 2.0. Default: 2.0.}

\item{min_effect_size}{Minimum absolute difference in proportions (%) required as a
descriptive filter. Default: 5.}

\item{min_fold_change}{Minimum fold change in proportions required as a descriptive filter.}
}
\description{
Tests whether the proportions of each level of a given factor differ
significantly between pairs of conditions. Uses an omnibus chi-square test on the
full contingency table for each pairwise comparison, then derives per-level
directionality from standardized Pearson residuals.
}
\author{
Geremy Clair
}
