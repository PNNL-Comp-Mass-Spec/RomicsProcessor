% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_Feature_Plots.R
\name{multipleFeatureComparisonPlot}
\alias{multipleFeatureComparisonPlot}
\title{multipleFeatureComparisonPlot()}
\usage{
multipleFeatureComparisonPlot(
  romics_object,
  features,
  point_type = "scatter",
  factor = "main",
  group_by = "feature",
  normalize = FALSE,
  group_spacing = 0.5,
  point_size = 2,
  element_width = 0.7,
  element_alpha = 0.7,
  title = "Feature Comparison",
  feature_labels = NULL,
  x_axis_label = NULL,
  y_axis_label = "Intensity",
  legend_title = NULL,
  palette = NULL,
  show_dividers = TRUE,
  divider_color = "gray70",
  divider_linetype = "dashed",
  group_order = NULL
)
}
\arguments{

\item{romics_object}{A romics_object created using romicsCreateObject().}

\item{features}{Character vector. Names of features to plot. Must match exactly (case-sensitive).}

\item{point_type}{Character. How to represent individual data points: "scatter", "boxplot", "violin", "bar". Default: "scatter"}

\item{factor}{Character. Factor name from romics_object to use for grouping. Use "main" for the main factor. Default: "main"}

\item{group_by}{Character. How to organize the plot: "feature" (features together) or "factor" (factors together). Default: "feature"}

\item{normalize}{Logical or character. Whether/how to normalize feature values:
FALSE (no normalization), "zscore", "minmax", or "percent_max". Default: FALSE}

\item{group_spacing}{Numeric. Space between groups (features or factors). Default: 0.5}

\item{point_size}{Numeric. Size of points for scatter representation. Default: 2}

\item{element_width}{Numeric. Width of boxplots/violins. Default: 0.7}

\item{element_alpha}{Numeric. Transparency for elements (0-1). Default: 0.7}

\item{title}{Character. Plot title. Default: "Feature Comparison"}

\item{feature_labels}{Character vector. Custom labels for features (same length as features). Default: NULL (use feature names)}

\item{x_axis_label}{Character. Label for the x-axis. Default: NULL (auto-set based on group_by)}

\item{y_axis_label}{Character. Label for the y-axis. Default: "Intensity" (changes based on normalization)}

\item{legend_title}{Character. Title for the legend. Default: NULL (auto-set based on group_by)}

\item{palette}{Character vector. Custom color palette for features/groups. Default: NULL (uses built-in palette)}

\item{show_dividers}{Logical. Whether to show dividing lines between groups. Default: TRUE}

\item{divider_color}{Character. Color for the dividing lines. Default: "gray70"}

\item{divider_linetype}{Character. Line type for dividers: "solid", "dashed", etc. Default: "dashed"}
}
\value{
A ggplot2 object
}
\description{
Creates a single plot comparing multiple features from a romics_object across groups.
}
\details{
This function allows plotting multiple features within the same plot for direct comparison.
Features can be normalized to enable comparison on the same scale. Various representation types are
supported for individual data points, including scatter, boxplots, violins, and bars.
}
\author{
Your Name
}
