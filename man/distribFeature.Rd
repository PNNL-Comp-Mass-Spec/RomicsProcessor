% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_Distribution_Plots.R
\name{distribFeature}
\alias{distribFeature}
\title{distribFeature()}
\usage{
distribFeature(
  romics_object,
  feature_name = NULL,
  bin = 0.1,
  show_quantiles = TRUE,
  threshold_value = NULL,
  threshold_color = "red",
  threshold_linetype = "dashed",
  threshold_size = 1,
  scale_x = NULL,
  scale_y = NULL,
  fill_color = NULL,
  alpha = 0.7,
  title = NULL,
  show_stats = TRUE
)
}
\arguments{

\item{romics_object}{A romics_object created using romicsCreateObject()}

\item{feature_name}{Character string. Name of the feature to plot. Must match a row name in the data exactly.}

\item{bin}{Numeric. Width of the histogram bins. Default: 0.1}

\item{show_quantiles}{Logical. Whether to display quartile lines (Q1, median, Q3) on the plot. Default: TRUE}

\item{threshold_value}{Numeric. Value at which to draw a vertical threshold line. If NULL, no threshold line is drawn. Default: NULL}

\item{threshold_color}{Character. Color of the threshold line. Default: "red"}

\item{threshold_linetype}{Character. Line type for threshold ("solid", "dashed", "dotted"). Default: "dashed"}

\item{threshold_size}{Numeric. Size/width of the threshold line. Default: 1}

\item{scale_x}{Numeric vector of length 2. X-axis limits c(min, max). If NULL, uses data range. Default: NULL}

\item{scale_y}{Numeric vector of length 2. Y-axis limits c(min, max). If NULL, auto-scales. Default: NULL}

\item{fill_color}{Character. Fill color for histogram. If NULL, uses sample colors from romics_object. Default: NULL}

\item{alpha}{Numeric. Transparency of histogram bars (0-1). Default: 0.7}

\item{title}{Character. Plot title. If NULL, uses feature name. Default: NULL}

\item{show_stats}{Logical. Whether to display summary statistics on the plot. Default: TRUE}
}
\value{
A ggplot2 object
}
\description{
Plots the distribution of a specific feature across all samples, with options to display quartiles and threshold lines.
}
\details{
This function creates a histogram showing the distribution of a specific feature across all samples.
Quartile lines show Q1 (25th percentile), median (50th percentile), and Q3 (75th percentile).
The threshold line can be used to visualize cutoff values or reference points.
Automatic log-transform detection adjusts axis labels appropriately.
}
\examples{
\dontrun{
# Basic feature distribution
distribFeature(romics_obj, feature_name = "Gene1")

# With threshold line at value 5
distribFeature(romics_obj, feature_name = "Gene1", threshold_value = 5)

# Custom styling with no quartiles
distribFeature(romics_obj, feature_name = "Gene1",
               show_quantiles = FALSE, fill_color = "blue",
               threshold_value = 2.5, threshold_color = "green")
}
}
\author{
Geremy Clair
}
