% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/21_data_interpretation_integration_functions.R
\name{romicsFuzzyCMeans}
\alias{romicsFuzzyCMeans}
\title{romicsFuzzyCMeans()}
\usage{
romicsFuzzyCMeans(
  romics_object,
  n_clusters = 3,
  m = 1.25,
  iter_max = 100,
  standardize = TRUE,
  method = "z-score",
  aggregate_by_factor = NULL
)
}
\arguments{

\item{romics_object}{A romics_object created using createRomicsObject()}

\item{n_clusters}{Numeric indicating the number of clusters to create (default: 3)}

\item{m}{Numeric indicating the fuzziness parameter (default: 1.25). Higher values increase fuzziness. Must be > 1}

\item{iter_max}{Numeric indicating maximum number of iterations (default: 100)}

\item{standardize}{Logical indicating whether to standardize features before clustering (default: TRUE). Recommended for comparing features with different scales}

\item{method}{Character indicating preprocessing method: "none", "z-score" (default), or "min-max"}

\item{aggregate_by_factor}{Character indicating metadata factor to aggregate samples by before clustering (default: NULL, no aggregation). When provided, samples are averaged within each factor level}
}
\value{
Returns a romics_object with new columns in the statistics layer:
- Cmeans_cluster: The assigned cluster for each feature (highest membership)
- Cmeans_membership_[n]: Membership values (0-1) for each cluster
}
\description{
Performs Fuzzy C-means clustering on features based on their abundance behavior across samples
}
\details{
This function performs Fuzzy C-means clustering to group features by their abundance patterns across samples.
Features are assigned to clusters with membership degrees (0-1) indicating their strength of membership.
Requires the 'Mfuzz' package to be installed. If not present, the function will prompt the user to install it.
The data is standardized before clustering to ensure all features contribute equally regardless of their absolute abundance values.
}
\examples{
# Basic Fuzzy C-means with 5 clusters
romics_object <- romicsFuzzyCMeans(romics_object, n_clusters = 5)

# More fuzzy clustering with custom parameters
romics_object <- romicsFuzzyCMeans(
  romics_object,
  n_clusters = 4,
  m = 1.5,
  method = "z-score"
)
}
\author{
Geremy Clair
}
