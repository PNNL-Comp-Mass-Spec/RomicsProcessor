% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_Statistics.R
\name{romicsTransferStatistics}
\alias{romicsTransferStatistics}
\title{romicsTransferStatistics()}
\usage{
romicsTransferStatistics(
  source_romics_object,
  target_romics_object,
  statistics = NULL
)
}
\arguments{
\item{source_romics_object}{A romics_object containing the statistics to transfer}

\item{target_romics_object}{A romics_object that will receive the statistics}

\item{statistics}{Character vector specifying which statistics columns to transfer (default: NULL, transfers all)
If NULL, all statistics columns from source will be transferred}
}
\value{
Returns the target_romics_object with transferred statistics added/updated
}
\description{
Transfers statistics columns from a source romics_object to a target romics_object, matching features by row name
}
\details{
This function matches features between source and target romics_objects by their row names.
Statistics columns are transferred for matching features. Features present in target but not in source will have NA values.
If target_romics_object does not have a statistics layer, one will be created.
}
\examples{
# Transfer all statistics from source to target
target_obj <- romicsTransferStatistics(
  source_romics_object = clustered_obj,
  target_romics_object = original_obj
)

# Transfer specific statistics columns
target_obj <- romicsTransferStatistics(
  source_romics_object = clustered_obj,
  target_romics_object = original_obj,
  statistics = c("Cmeans_cluster", "Cmeans_membership_C1")
)
}
\author{
Geremy Clair
}
