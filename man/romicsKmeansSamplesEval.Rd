% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_Grouping-WE45756.R, R/07_Grouping.R
\name{romicsKmeansSamplesEval}
\alias{romicsKmeansSamplesEval}
\title{romicsKmeansSamplesEval()}
\usage{
romicsKmeansSamplesEval(
  romics_object,
  iter.max = 100,
  algorithm = c("Lloyd", "Hartigan-Wong", "Forgy", "MacQueen"),
  cluster_using = c("data", "pca", "umap", "tsne"),
  max_clust = 50,
  ...
)

romicsKmeansSamplesEval(
  romics_object,
  iter.max = 100,
  algorithm = c("Lloyd", "Hartigan-Wong", "Forgy", "MacQueen"),
  cluster_using = c("data", "pca", "umap", "tsne"),
  max_clust = 50,
  ...
)
}
\arguments{
\item{romics_object}{A romics_object created using romicsCreateObject()}

\item{iter.max}{The maximum number of iterations allowed. Default: 100}

\item{algorithm}{Character string specifying the K-means algorithm to use:
'Hartigan-Wong', 'Lloyd', 'Forgy', or 'MacQueen'. Default: 'Lloyd'}

\item{cluster_using}{Character string specifying which data to use for clustering:
'data' (full data matrix), 'pca' (PCA embeddings), 'umap' (UMAP embeddings), or 'tsne' (t-SNE embeddings). Default: 'data'}

\item{max_clust}{Maximum number of clusters to evaluate. Default: 50}

\item{...}{Additional parameters passed to kmeans().}
}
\value{
Returns a plot showing the WCSS for clusters from 1 to max_clust.

Returns a plot showing the WCSS for clusters from 1 to max_clust.
}
\description{
Evaluates the optimal number of clusters for K-means using the elbow method.
Plots the within-cluster sum of squares (WCSS) for different numbers of clusters.

Evaluates the optimal number of clusters for K-means using the elbow method.
Plots the within-cluster sum of squares (WCSS) for different numbers of clusters.
}
\author{
Geremy Clair
}
