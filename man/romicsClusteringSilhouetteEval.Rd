% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/21_data_interpretation_integration_functions.R
\name{romicsClusteringSilhouetteEval}
\alias{romicsClusteringSilhouetteEval}
\title{romicsClusteringSilhouetteEval()}
\usage{
romicsClusteringSilhouetteEval(
  romics_object,
  method = "kmeans",
  max_clusters = 10,
  standardize = TRUE,
  standardization = "z-score",
  iter_max = 100,
  m = 1.25,
  nstart = 25,
  aggregate_by_factor = NULL
)
}
\arguments{

\item{romics_object}{A romics_object created using createRomicsObject()}

\item{method}{Character indicating clustering method: "kmeans" or "fuzzy_cmeans" (default: "kmeans")}

\item{max_clusters}{Numeric indicating maximum number of clusters to evaluate (default: 10)}

\item{standardize}{Logical indicating whether to standardize data before clustering (default: TRUE)}

\item{standardization}{Character indicating standardization method: "z-score", "min-max", or "none" (default: "z-score")}

\item{iter_max}{Numeric indicating maximum number of iterations for k-means (default: 100)}

\item{m}{Numeric fuzziness parameter for fuzzy c-means (default: 1.25, only used if method="fuzzy_cmeans")}

\item{nstart}{Numeric for k-means: number of random initializations (default: 25)}

\item{aggregate_by_factor}{Character indicating metadata factor to aggregate samples by before clustering (default: NULL, no aggregation). When provided, samples are averaged within each factor level}
}
\value{
Returns a ggplot object showing silhouette scores for each cluster number
}
\description{
Evaluates the optimal number of clusters for feature clustering using silhouette analysis.
Supports both k-means and fuzzy c-means clustering. Silhouette coefficient ranges from -1 to 1,
with values closer to 1 indicating well-separated clusters.
}
\details{
Calculates the mean silhouette coefficient for each cluster number.
Higher silhouette values indicate better-defined clusters. The cluster number with the
highest average silhouette coefficient is typically optimal.
}
\examples{
# Evaluate k-means clustering with silhouette method
plot <- romicsClusteringSilhouetteEval(
  romics_object,
  method = "kmeans",
  max_clusters = 10
)
plot

# Evaluate fuzzy c-means clustering
plot <- romicsClusteringSilhouetteEval(
  romics_object,
  method = "fuzzy_cmeans",
  max_clusters = 8
)
plot
}
\author{
Geremy Clair
}
