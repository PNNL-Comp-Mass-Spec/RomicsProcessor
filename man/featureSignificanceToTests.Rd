% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_Statistics.R
\name{featureSignificanceToTests}
\alias{featureSignificanceToTests}
\title{featureSignificanceToTests()}
\usage{
featureSignificanceToTests(
  romics_object,
  feature,
  ptype = c("padj", "p"),
  p_threshold = 0.05,
  test_types = NULL,
  specific_comparisons = NULL
)
}
\arguments{
\item{romics_object}{A romics_object with calculated statistics}

\item{feature}{Character string of feature name (must match a rowname in romics_object$data)}

\item{ptype}{Character string indicating which p-value type to use: "padj" for adjusted p-values or "p" for raw p-values. Default: "padj"}

\item{p_threshold}{Numeric value for significance threshold. Default: 0.05}

\item{test_types}{Character vector of test types to consider (e.g., c("Ttest", "Wilcox_test", "glmBinomialTest")). If NULL, considers all tests. Default: NULL}

\item{specific_comparisons}{Character vector of specific comparisons to consider (e.g., "Alport_vs_Ctrl"). If NULL, considers all comparisons. Default: NULL}
}
\value{
Data frame with columns: test_name (character), significant (logical), direction (character: "up"/"down"/NA)
}
\description{
Evaluate which statistical tests find a specific feature significant
}
\details{
Returns a data frame with one row per test, showing whether the feature is significant.
         Directionality (up/down) is included for pairwise comparison tests (t-test, Wilcox, GLM binomial)
         but not for multi-group tests (ANOVA). Direction is NA for non-significant results.
}
\examples{
\dontrun{
  # Get significance across all tests for a specific feature
  results <- featureSignificanceToTests(romics_proteins, feature = "ProteinA")

  # Get significance for t-tests only
  results <- featureSignificanceToTests(romics_proteins, feature = "ProteinA", test_types = "Ttest")

  # Use raw p-values instead of adjusted
  results <- featureSignificanceToTests(romics_proteins, feature = "ProteinA", ptype = "p")
}
}
\author{
Geremy Clair
}
