% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_Grouping-WE45756.R, R/07_Grouping.R
\name{romicsNameClusters}
\alias{romicsNameClusters}
\title{romicsNameClusters()}
\usage{
romicsNameClusters(
  romics_object,
  cluster_factor = NULL,
  cluster_new_names = NULL,
  new_factor_name = NULL,
  alphabetical_mapping = FALSE
)

romicsNameClusters(
  romics_object,
  cluster_factor = NULL,
  cluster_new_names = NULL,
  new_factor_name = NULL,
  alphabetical_mapping = FALSE
)
}
\arguments{
\item{romics_object}{A romics_object created using romicsCreateObject()}

\item{cluster_factor}{Character string specifying the name of an existing factor in metadata containing the clusters to rename.
Must be a valid factor name that can be verified with romicsFactorNames().
If missing, defaults to the main factor of the romics_object.}

\item{cluster_new_names}{Optional character vector containing the new names for each cluster.
If provided, must have the same length as the number of unique clusters.
If NULL (default), the function will interactively prompt for names for each cluster.}

\item{new_factor_name}{Optional character string. If provided, creates a new factor with this name instead of modifying the existing factor.
The original factor remains unchanged. If NULL (default), modifies the existing factor.}

\item{alphabetical_mapping}{Logical. If TRUE and cluster_new_names is provided, maps new names alphabetically to original clusters.
If FALSE (default), maps based on the order of appearance or natural sorting of original clusters.}
}
\value{
Returns the romics_object with renamed clusters and the original clusters preserved (if modifying existing factor)

Returns the romics_object with renamed clusters and the original clusters preserved (if modifying existing factor)
}
\description{
Rename clusters in any factor of a romics_object's metadata.
The original factor values are preserved in a new row with "_original" appended to the factor name.
Optionally create a new factor instead of modifying the existing one.

Rename clusters in any factor of a romics_object's metadata.
The original factor values are preserved in a new row with "_original" appended to the factor name.
Optionally create a new factor instead of modifying the existing one.
}
\author{
Geremy Clair
}
