% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_Heatmap_and_multifeature_plots.R
\name{romicsComplexHeatmap}
\alias{romicsComplexHeatmap}
\title{romicsComplexHeatmap()}
\usage{
romicsComplexHeatmap(
  romics_object,
  comparison_prefix = NULL,
  factor_name = NULL,
  feature_list = NULL,
  p_threshold = 0.05,
  use_adjusted_p = TRUE,
  scale_data = TRUE,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_row_names = TRUE,
  show_column_names = TRUE,
  heatmap_colors = c("blue", "white", "red"),
  color_breaks = NULL,
  allow_missing = TRUE,
  missing_color = "grey",
  significance_color = "red",
  collapse_by_factor = NULL
)
}
\arguments{
\item{romics_object}{A romics object containing $data, $statistics, and $metadata layers}

\item{comparison_prefix}{Character string of the comparison prefix (e.g., "Alveoli_pleura_vs_Bronchi_bronchiole"). If NULL, auto-detects from statistics columns}

\item{factor_name}{Factor name from metadata for sample grouping. If NULL, uses main factor. Use romicsFactorNames() to see available factors}

\item{feature_list}{Character vector of features to include in heatmap. If provided, creates subset using romicsFeatureSubset(). If NULL, uses all features}

\item{p_threshold}{Numeric threshold for p-values (default 0.05)}

\item{use_adjusted_p}{TRUE or FALSE, if TRUE uses adjusted p-values, if FALSE uses raw p-values}

\item{scale_data}{TRUE or FALSE, if TRUE scales the data by rows (z-score)}

\item{cluster_rows}{TRUE or FALSE, if TRUE performs hierarchical clustering of rows}

\item{cluster_cols}{TRUE or FALSE, if TRUE performs hierarchical clustering of columns}

\item{show_row_names}{TRUE or FALSE, if TRUE shows feature names on rows}

\item{show_column_names}{TRUE or FALSE, if TRUE shows sample names on columns}

\item{heatmap_colors}{Character vector of colors for the heatmap. Default: c("blue", "white", "red")}

\item{color_breaks}{Numeric vector of breaks for color mapping. If NULL, uses automatic breaks}

\item{allow_missing}{TRUE or FALSE, if TRUE allows missing values in the heatmap}

\item{missing_color}{Color for missing values in the heatmap (default: "grey")}

\item{significance_color}{Color for significant results in all test annotations (default: "red")}
}
\value{
ComplexHeatmap object
}
\description{
Creates a heatmap with statistical annotations from a romics object
}
\details{
Creates a heatmap with side annotations automatically detected from statistics columns (T-test, Wilcoxon, GLM binomial, ANOVA) and sample groups from metadata. If feature_list is provided, automatically subsets the romics object first.
}
\author{
Geremy Clair
}
